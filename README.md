# 集思居家風格測驗

參考 purely2006 的「生活測驗」做法，但機制與內容全部重寫成集思自己的版本。
純靜態網頁，**沒有後端、沒有資料庫、沒有月費**。

## 網址

| | 連結 |
|---|---|
| **測驗（給客人）** | https://fooicebo-wq.github.io/home-style-quiz/ |
| **後台（給你）** | https://fooicebo-wq.github.io/home-style-quiz/admin.html |
| 原始碼 | https://github.com/fooicebo-wq/home-style-quiz |

本機資料夾：`E:\★★★Claude 資料總存區\居家風格測驗\`

---

## 檔案結構

| 檔案 | 做什麼 | 你會不會動到 |
|---|---|---|
| `index.html` | 測驗本體（版面 + 計分邏輯） | 改配色時才動 |
| `quiz-data.js` | 題目、16 種原型文字、預設插圖 | 要改結果文字時動這裡 |
| `content.js` | **後台匯出的覆寫檔**（照片、標籤、題目） | 後台自動產生，直接覆蓋 |
| `admin.html` | **後台**：換照片、改文字 | 你平常只開這個 |

`content.js` 不存在或是空的也能正常運作，測驗會自動用 `quiz-data.js` 的內建插圖。

---

## 換照片、改文字（後台）

1. 打開後台 → https://fooicebo-wq.github.io/home-style-quiz/admin.html
2. **換照片**：點任何一張圖 → 選你的案場照片
   - 自動**置中裁成正方形**、縮到 640px、轉 JPEG，不必事先修圖
   - 也可以直接把圖**拖進格子**
3. **改文字**：題目標題、選項名稱直接在輸入框改；
   結果卡的 16 種原型在頁面最下方，點標題展開編輯（改過的會標成金色）
4. 按 **預覽測驗** 確認畫面
5. 按 **匯出 content.js** → 下載的檔案**覆蓋**資料夾裡的 `content.js`
6. 用下面「上線更新」把它推上去

**不必一次換完。** 沒換照片的格子會自動用內建插圖，混著用尺寸也一致。
原型文字同理，只改「一句標語」也可以，其他欄位沿用預設。

---

## 選項圖現況（2026-09-07）

用 Gemini `gemini-3-pro-image` 生成，**同一組 8 張共用一個場景錨點、只換主體**。
生成的圖沒有版權問題，可直接商用。

| 題目 | 狀態 |
|---|---|
| 1 畫作 | 照片 8/8 |
| 2 椅子 | 照片 8/8 |
| 3 燈具 | 內建插圖（生成的 8 張已否決，見下） |
| 4 花器 | 照片 8/8 |
| 5 杯子 | 內建插圖（未生成） |
| 6 顏色 | 色票，不需圖 |
| 7 窗景 | 內建插圖（未生成） |
| 8 門把 | 內建插圖（未生成） |

**為什麼停在這裡：** Google AI Studio 的**每月消費上限用完**，剩下 23 張生不出來。
要繼續生，先到 https://ai.studio/spend 調高上限。

**燈具那 8 張為什麼不用：** 第 3 張「軌道投射燈」畫面主角變成桌上的檯燈、沒拍到主體，
而且跟第 7 張「陶土桌燈」幾乎一樣。測驗要讓人二選一，兩格長得一樣是硬傷。
檔案留在 `images/_review/`（不會上線），要看可以自己判斷。

### 額度恢復後怎麼把剩下的做完

`Gemini生成指令.md` 裡有全部 56 段指令。還缺的是：
- **燈具 8 張**（重生，錨點要改成「完全空的房間、不准有傢俱」，並把桌燈類改成放在單一基座上）
- **杯子 8 張、窗景 8 張、門把 8 張**

生完丟進後台上傳，或直接把檔案放進 `images/` 並依 `組名-編號.jpg` 命名
（例如 `cup-3.jpg`），再把 `content.js` 對應的那組補上路徑即可。

**一題之內必須全是照片或全是插圖**，混用會很醜——這是刻意的設計限制。

---

## 上線更新

換完照片、覆蓋好 `content.js` 之後，在資料夾裡執行：

```bash
cd "E:/★★★Claude 資料總存區/居家風格測驗" && git add -A && git commit -m "更新照片" && git push
```

推上去後約 **1～2 分鐘**線上就會更新（GitHub Pages 要重建）。
沒更新的話按 Ctrl+F5 強制重新整理。

### 後台的其他按鈕
- **匯入**：把舊的 `content.js` 讀回後台繼續編輯（換電腦、或草稿被清掉時用）
- **全部清除**：清掉草稿回到預設，已匯出的 `content.js` 不受影響

### 容量上限
草稿存在瀏覽器的 localStorage，上限約 5MB，後台頂端會顯示目前容量。
64 格全換成照片約 1～1.5MB，正常用不會爆。超過會擋下來並提示。

---

## 測驗機制

- 8 題，每題 8 個選項；第 6 題是色票（不能換照片）
- 每個選項綁 **2 個價值標籤**：主標籤 +2、副標籤 +1，再乘題目權重
- 題目權重遞減：**1.3 → 1.3 → 1.0 → 1.0 → 0.9 → 0.8 → 0.7 → 0.6**（前面的題目影響力大）
- 價值共 10 軸：沉靜、秩序、溫度、自然、工藝、個性、玩心、社交、質感、機能
- 16 種原型各綁 2 個價值，比對公式：`主值×2 + 副值×1`，取最高分
- 排第 2 名的顯示為「你的另一面」

改計分只要動 `quiz-data.js` 裡的 `v: ["主","副"]` 和 `w`（權重）。

---

## 導流與追蹤（原版沒做的部分）

### LINE
結果頁的 LINE 按鈕會**把測驗結果直接帶進訊息裡**，客人一按送出，你收到的訊息長這樣：

> 我的居家原型是【木作職人派・Wood Artisan】
> 「好東西，值得等」
> 想聊聊我家可以怎麼做。

LINE 帳號改在 `quiz-data.js` 的 `CONFIG.lineOA`（或後台的「品牌設定」）。
目前設定：`%40608iofzm`（@ 要寫成 `%40`）。

### 追蹤事件
`track()` 會同時送三個地方，有裝就會收到，沒裝也不會出錯：
- **GA4**（`gtag`）
- **FB Pixel**（`fbq`）
- **`postMessage` 給外層網頁** ← 嵌在別的網站的 iframe 裡也追蹤得到

送出的事件：`quiz_start`、`quiz_answer`、`quiz_complete`、`quiz_share`、`quiz_cta_line`、`quiz_cta_site`。
`quiz_complete` 帶 `result`（原型）和 `path`（他一路選了什麼），可以直接看出哪種客人最多。

要收 iframe 的事件，在外層網站放：

```js
window.addEventListener('message', e => {
  if (e.data && e.data.source === 'gis-quiz' && window.gtag) {
    gtag('event', e.data.event, e.data.params);
  }
});
```

---

## 本機測試（通常用不到，直接用線上後台就好）

```bash
python -m http.server 5211 --directory "E:/★★★Claude 資料總存區/居家風格測驗"
```

然後開 http://localhost:5211/admin.html

> **不要直接雙擊 html 開。** 後台要存草稿（localStorage），`file://` 開啟時瀏覽器會擋掉。
> 線上版是 https，沒這問題，所以平常用線上後台最省事。

---

## 已知限制

- **色票題不能換照片**（它本來就是色塊，不是圖）
- 草稿存在「你這台電腦的這個瀏覽器」裡。換電腦要用後台的 **匯入** 把 `content.js` 讀回來接著編
- 照片存在 `content.js` 裡（base64），64 張全換約 1～1.5MB。想更輕可以改成放圖片網址：
  把 `content.js` 裡的 `"img": "data:image/jpeg;base64,..."` 換成 `"img": "images/xxx.jpg"`
